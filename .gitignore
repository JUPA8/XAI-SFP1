# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
*.egg

# macOS
.DS_Store

# Datasets — too large for git (GTA5: 5.7GB, Cityscapes: 11GB, gtFine: 808MB)
images/
leftImg8bit_trainvaltest/
gtFine_trainvaltest/

# Model checkpoints — too large for git (144MB); store on HPC or use Git LFS
*.pt
*.pth

# Training run artifacts — reproducible from training
splits.json
training_history.json

# Attribution outputs — generated by Step F
outputs/attributions/
